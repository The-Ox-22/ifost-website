# Base URL for the gas-tracker service (see /home/ianfoster/dev/gas-tracker).
# Defaults to the in-cluster ClusterIP DNS name if unset - only needed for
# local dev, e.g. after `kubectl port-forward svc/gas-tracker 8080:80 -n gas-tracker`:
# GAS_TRACKER_URL=http://localhost:8080
